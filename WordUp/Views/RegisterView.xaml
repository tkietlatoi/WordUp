<UserControl x:Class="WordUp.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WordUp">
    <Grid Margin="18,16,18,20">
        <Border Style="{StaticResource Card}"
                Width="360"
                MaxWidth="360"
                VerticalAlignment="Center"
                Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <Border Width="58" Height="58" CornerRadius="12" Background="#F0EEFA" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <TextBlock Text="WU" FontSize="21" FontWeight="Bold" Foreground="{StaticResource IndigoBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="WordUp" Style="{StaticResource PageTitleText}" TextAlignment="Center" />
                    <TextBlock Text="T&#7841;o t&#224;i kho&#7843;n h&#7885;c t&#7853;p c&#7911;a b&#7841;n" Style="{StaticResource MutedText}" TextAlignment="Center" Margin="0,4,0,16" />

                    <Grid Width="220" HorizontalAlignment="Center" Margin="0,0,0,14">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="34" />
                            <RowDefinition Height="4" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Button Content="&#272;&#259;ng nh&#7853;p"
                                Style="{StaticResource AuthTabButton}"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="Login" />
                        <Button Grid.Column="1"
                                Content="&#272;&#259;ng k&#253;"
                                Style="{StaticResource ActiveAuthTabButton}" />
                        <Border Grid.Row="1"
                                Grid.Column="1"
                                Width="54"
                                Height="3"
                                Background="{StaticResource IndigoBrush}"
                                CornerRadius="2"
                                HorizontalAlignment="Center" />
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="T&#234;n c&#7911;a b&#7841;n" Margin="0,0,0,5" />
                    <TextBox Style="{StaticResource TextInput}" Text="{Binding RegisterFullName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                    <TextBlock Text="Email" Margin="0,0,0,6" />
                    <TextBox Style="{StaticResource TextInput}" Text="{Binding RegisterEmail, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                    <TextBlock Text="S&#7889; &#273;i&#7879;n tho&#7841;i" Margin="0,0,0,5" />
                    <TextBox Style="{StaticResource TextInput}" Text="{Binding RegisterPhone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                    <TextBlock Text="M&#7853;t kh&#7849;u" Margin="0,0,0,5" />
                    <PasswordBox Style="{StaticResource PasswordInput}" local:PasswordBoxHelper.BoundPassword="{Binding RegisterPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4" />
                    <TextBlock Text="T&#7889;i thi&#7875;u 8 k&#253; t&#7921;" Style="{StaticResource MutedText}" FontSize="11" Margin="0,0,0,8" />
                    <TextBlock Text="X&#225;c nh&#7853;n m&#7853;t kh&#7849;u" Margin="0,0,0,5" />
                    <PasswordBox Style="{StaticResource PasswordInput}" local:PasswordBoxHelper.BoundPassword="{Binding RegisterConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding AuthMessage}" Foreground="{StaticResource DangerBrush}" FontSize="12" Margin="0,0,0,8" />
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <Button Content="Ti&#7871;p t&#7909;c" Style="{StaticResource PrimaryButton}" Command="{Binding RegisterCommand}" />
                    <Button Content="Quay v&#7873; Trang ch&#7911;" Style="{StaticResource TextLinkButton}" HorizontalAlignment="Center" Margin="0,16,0,0" Command="{Binding SelectTabCommand}" CommandParameter="Dashboard" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
